// Pure SPA mode: disable server-side rendering for the whole app.
// adapter-static (fallback: 'index.html') serves a single entry page
// and all routing happens client-side.
export const ssr = false;
